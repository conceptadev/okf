name: okf_release
description: Release tooling for the OKF command-line package.
publish_to: none

environment:
  # cli_pkg 2.15.2 uses the Dart 3.11-only xml 7 API.
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  cli_pkg: 2.15.2
  grinder: 0.10.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.32.0
